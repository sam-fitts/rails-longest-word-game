<h1>Result</h1>

<% if @included %>
  <% if @in_dictionary %>
    <p><strong>Congratulations!</strong> <%= @word %> is in the dictionary!</p>
  <% else %>
    <p>Sorry but <strong><%= @word %></strong> is not a valid word...</p>
  <% end %>
<% else %>
  <p>Sorry but <strong><%= @word %></strong> is not the same as the letters contained in <%= @letters.join(", ") %></p>
<% end %>

<%= link_to "Play again", game_new_path %>
